# <システム名> 受け入れシナリオ — スライス<スライスID>「<ユースケース名>」
# ステータス: 承認待ち(人間が承認すると、このファイルが正式な仕様になる)
# <スライスID>-xx はシナリオの通し番号。テストがどのシナリオを検証しているかの突き合わせに使う。欠番は再利用しない

Feature: <ユースケース名(業務の言葉)>
  <誰>として、<何をしたい>。
  <なぜ>のためである。

  Background:
    Given <全シナリオ共通の前提。関連パラメータは括弧でまとめて1 stepに>

  Rule: <業務ルール(正常系)>

    # <スライスID>-01
    Scenario: <一文のシナリオ名>
      When <アクターの操作>
      Then <業務的に観測可能な結果>

  # 出典: <このルールの根拠(不変条件・ワーク素材・ADR)への参照>
  Rule: <業務ルール(境界や排他などの中核ルール)>

    # <スライスID>-02: <境界ケースの意図をここに一言>
    Scenario: <一文のシナリオ名>
      Given <前提状態>
      When <アクターの操作>
      Then <業務的に観測可能な結果>

  # 出典: <...>
  Rule: <同型の拒否ケース群を束ねるルール>

    Scenario Outline: <ケース>予約は作成できない  # ←動詞は対象に合わせて置換
      When <操作。可変部分は"<プレースホルダ>">
      Then <結果は"<拒否の理由>"という理由で拒否される、の形>

      Examples:
        | ID             | ケース   | <入力列...> | 拒否の理由 |
        | <スライスID>-03 | <一言>   | ...         | <業務の言葉の理由文> |

# ---- 記入のコツ(コピー後にこのブロックは削除する) ----
# - シナリオ本文は業務の言葉のみ。技術用語禁止(verification.md L4)
# - ヘッダは読者(承認者)向けの最小情報だけ。書き手向け規約をここに書き写さない(P-04)
# - キーワードは英語Gherkin+日本語文。行頭が揃い構造が追える(meta/adr/0002)
# - 同じ形の拒否ケースはScenario Outline+例表に畳む。言い回しが違うものだけ個別に書く
# - 起草したら全シナリオをAPIスキーマのリクエスト/レスポンス具体値に落としてみる。
#   表現できないシナリオ・同じ入力に別の結果を要求するシナリオは契約の矛盾(FR-005の教訓)
# - 理由文はAPI仕様の理由コードと1対1対応させ、対応表をAPI仕様側の冒頭コメントに置く
# - API仕様(OpenAPI)側にも冒頭に承認者向けサマリを置く: 3行程度の要約(何をどう呼ぶと何が返るか、平易な言葉で)+
#   代表的なリクエスト/成功レスポンス/拒否レスポンスの具体例。詳細なスキーマ定義は後続に置く(meta/adr/0016)
