# meta/scenario-id-prefixes.md — シナリオIDプレフィックス台帳

> 対象: 全プロジェクトで受け入れシナリオ（`.feature`）を起票する前。
> シナリオIDの正規表現は `[A-Z]{2,}-[A-Z]-\d{2}`（例: `RSV-C-01`。`meta/tools/govlint.py` `SCENARIO_ID`）。
> ハイフン前の**プレフィックス**（2文字以上の英大文字列）はプロジェクトごとに一意に予約する。
> 根拠: `meta/adr/0026`（複数プロジェクトの並行開発の制度化、決定3）。

## 予約済みプレフィックス

| プレフィックス | プロジェクト | 登録日 | 備考 |
|---|---|---|---|
| `RSV` | `projects/reservation-system` | 2026-07-14 | 会議室予約バックエンド。`RSV-A`(空き状況)/`RSV-C`(作成)/`RSV-K`(キャンセル)/`RSV-R`(規則)等、ハイフン後の1文字（サブレター）はプロジェクト内で自由に使ってよい |
| `RFE` | `projects/reservation-frontend` | 2026-07-22 | 会議室予約フロントエンド。`RFE-A`(空き状況画面)/`RFE-B`(予約作成画面)等 |
| `TWR` | `projects/toyama-weekend-radar` | 2026-07-29 | 富山市近郊の週末イベント提案。`TWR-W`(週末イベント一覧)等 |
| `TDR` | `projects/toyama-dining-radar` | 2026-07-29 | 富山県庁周辺の月例ランチ会向け飲食店提案。`TDR-L`(ランチ候補提案)等 |
| `CSR` | `projects/connpass-session-radar` | 2026-08-15 | connpassの条件に合うイベントを毎朝ダイジェストで通知する。**画面を持たない**。ハイフン後のサブレターは契約の起票時に決める。**登録当初の「Connpassの興味深いセッションを見つける画面。`CSR-B`(セッション閲覧)」から2026-08-16に書き換えた**——人間の判断でスコープを画面から通知へ改めたためであり、プレフィックス `CSR` 自体とプロジェクト名は変更していない |

## 新規プロジェクトがプレフィックスを予約する手順

1. 上記表に無い、2文字以上の英大文字プレフィックスを選ぶ。既存と紛らわしい類似形（例: 既存`RSV`に対する`RSU`のような1文字違い）は、人間が読んで識別する助けにならないため避ける
2. 本ファイルに1行追記するPRを出す。このPRは他の`meta/**`変更と同様に**直列化の対象**（`meta/adr/0026` 決定4）
3. 追記が承認・マージされて初めて、そのプレフィックスで`.feature`にシナリオIDを書き始めてよい

## 機械検証との関係（本台帳が埋めるCIの死角）

本台帳自体は`meta/tools/govlint.py`の検証対象ではない。govlintのシナリオID整合チェック
（`check_scenario_ids`）は**プロジェクトごとに独立**した`contracts/`ディレクトリ単位で定義・参照を
検証するため、**プレフィックスの重複そのものはCIでは検出されない**——プロジェクトAとプロジェクトBが
偶然同じプレフィックスを使っても、両者は別々に走査され、govlintはどちらのPRでも緑のままになりうる。

本台帳は、この機械検証の死角を人間可読の一覧として埋めるものである（`meta/PRINCIPLES.md` P-03
「実行可能 > 機械検証可能 > 人間可読」の最下段）。プレフィックス重複を機械的に検出する仕組み（例:
govlintを拡張し全プロジェクト横断でも一意性を見る）は将来の強化候補として記録するに留め、現時点では
実装しない（P-05: 摩擦が発生してから足す。現時点でプレフィックス重複が実際に起きた実績はない）。
