# activeContext.md — Skill Map

> P-11: このファイルは常に現在だけを映す。履歴はgitとADRが持つ。

## 現在

**保留。着手しない。** 成果物は `product-brief.md` の1本だけで、コードも契約もADRも無い。
ブリーフは2026-08-23に承認されたが、**その直後に規約を読んで、いまのAPIキーでは作れないと分かった**。

**止めているのは公開の可否ではなく、APIキーの利用目的の範囲である。** API利用規約 第9条第1項は
「利用者が同意した利用目的以外の目的で利用者情報を利用してはならない」と定める。**参加履歴の取得は、
いまのキーが許された範囲に含まれていない。** しかも**個人申請ではキーを1本しか持てない**（追加不可）
ため、用途ごとに申請し直して逃げることもできない。

規約の読みそのものは無駄になっていない。**再配布については許す条文も禁じる条文も無いこと**、
**イベント名や概要の知的所有権はconnpassに帰属すること**（サイト利用規約 第4条）が分かっており、
目的の問題が解けたあとの出口の見立て——公開するのは自分の行動から導いた数字だけにする——まで
product-brief §4 に書いてある。

`project/skill-map` ブランチと保護ruleset（`protect project/skill-map`）は2026-08-23に作成済み
（`meta/adr/0028`。人間がchatでauthorizeした）。**シナリオIDのプレフィックスは未予約**——実体が
定まってから台帳に書く（product-brief §9-5に理由）。

## 再開の条件

**connpassへ問い合わせて、いまのキーの利用目的に自分自身の参加履歴の取得を追加できること。** これ1つ
だけである。**断られたらこのプロジェクトは成立しない**——スクレイピングは規約で禁止されており、
API以外に取得手段が無い。

再開できたら、次は `attended_events` の実測である——「attended」が参加登録を指すのか受付を指すのか、
キャンセルがどう見えるかの定義がAPIの文書に無い。**この一点で地図の意味が変わる**。

## 外部の事実

connpass API v2 の一次情報は **`projects/connpass-session-radar/connpass-api-v2-facts.md`** が持つ。
本プロジェクトは再定義せず参照する。特に効くのは次の3点である。

- `GET /users/{nickname}/attended_events/` と `.../presenter_events/` は `start` と `count` しか
  受け取らない。**日付で絞れない**ので、期間を限るなら全件取ってから絞る
- 返るのはイベント検索と同じ完全なイベントオブジェクト（`hash_tag`・`group`・`lat`/`lon` を含む）
- 1秒1リクエスト・1回100件。履歴300件で約3秒

## 保留

- **バックエンドは組まない**（現時点の判断）。履歴は多くても数百件で、集計はブラウザに全部載せて
  済む規模である。認証・他人のデータ・書き込みのどれかが要るようになったら改めて判断する
- **connpass以外のソース**（登壇・資格・業務経歴・書籍）は入れない。connpassだけで地図が成立するかを
  先に確かめる
