# Set these as GitHub Actions secrets for scheduled production runs. Do not commit values.
CONNPASS_API_KEY=
# Use an Incoming Webhook for a standard text channel. Forum channels require thread routing and are unsupported.
DISCORD_WEBHOOK_URL=
