[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "dining-radar"
version = "0.1.0"
description = "An invite-only lunch candidate organizer application."
requires-python = ">=3.12,<3.13"
dependencies = [
    "Django>=5.2.16,<5.3",
    "dj-database-url>=3.0,<4",
    "gunicorn>=23,<24",
    "psycopg[binary]>=3.2,<4",
    "whitenoise[brotli]>=6.9,<7",
]

[project.optional-dependencies]
dev = [
    "coverage[toml]>=7.15,<8",
    "jsonschema>=4.25,<5",
    "playwright>=1.48,<2",
    "pytest>=9.1,<10",
    "pytest-django>=4.12,<5",
    "pytest-gremlins>=1.9,<2",
    "PyYAML>=6.0.2,<7",
    "ruff>=0.15,<0.16",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "dining_radar.settings_test"
pythonpath = ["src"]
python_files = ["test_*.py"]
addopts = ["-ra", "--strict-markers"]

[tool.coverage.run]
branch = true
source = ["dining_radar"]
omit = [
    "*/migrations/*",
    "*/wsgi.py",
]

[tool.coverage.report]
fail_under = 90
show_missing = true
skip_covered = true

[tool.pytest-gremlins]
paths = [
    "src/dining_radar/authentication/forms.py",
    "src/dining_radar/authentication/throttle.py",
    "src/dining_radar/test_support/views.py",
    "src/dining_radar/web/views.py",
    "src/dining_radar/web/serializers.py",
    "src/dining_radar/recommendation/pipeline.py",
    "src/dining_radar/integrations/hotpepper/config.py",
    "src/dining_radar/integrations/hotpepper/client.py",
    "src/dining_radar/integrations/hotpepper/normalize.py",
    "src/dining_radar/suggestions/service.py",
    "src/dining_radar/suggestions/rate_limit.py",
    "src/dining_radar/suggestions/acceptance_state.py",
    "src/dining_radar/suggestions/hotpepper_source.py",
]
exclude = ["**/__pycache__/*"]
report = ["json"]
max_pardons = 0
